import { NoSpacesDirective } from './no-spaces.directive';

describe('NoSpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
