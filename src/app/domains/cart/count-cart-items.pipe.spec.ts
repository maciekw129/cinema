import { CountCartItemsPipe } from './count-cart-items.pipe';

describe('CountCartItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new CountCartItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
